var removeElement = function (nums, val) {
  var arrlen = nums.length;
  var count = 0;
  for (var i = 0; i < arrlen; i++) {

    if (nums[i] !== val) {
      nums[count] = nums[i];
      count++;
    }
  }
  return count;
};
